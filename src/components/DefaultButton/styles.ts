import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  socialItemContainer: {
    borderRadius: 12,
    overflow: 'hidden',
    marginHorizontal: 10,
    marginTop: 10,
  },
  socialItem: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 15,
  },
  socialItemText: {
    marginLeft: 10,
    fontSize: 16,
    fontWeight: 'bold',
  },
});

export default styles;
