import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  contentScrollView: {
    paddingBottom: 10,
  },
  socialItemContainer: {
    borderRadius: 12,
    overflow: 'hidden',
    marginHorizontal: 10,
    marginTop: 10,
  },
  socialItem: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 15,
  },
  socialItemText: {
    marginLeft: 10,
  },
});

export default styles;
